import { DEFAULT_LOCALE } from './i18n'

export const APP_CONFIG = {
    name: 'AutoCare Hub',
    tagline: 'Сервис, которому доверяют',
    description: 'Compare automotive services, prices, ratings, and availability.',
    currency: 'EUR',
    currencySymbol: '€',
    defaultLocale: DEFAULT_LOCALE
} as const
