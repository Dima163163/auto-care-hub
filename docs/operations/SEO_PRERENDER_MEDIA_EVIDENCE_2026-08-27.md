# SEO, prerender and media budget evidence

Run date: 2026-08-27

## Repository and local production-preview checks

- `npm run build` — passed with Next.js 16.3.2; 15 static pages generated.
- `npm run check:seo` — repository budgets and prerender contract passed.
- `SEO_BASE_URL=http://127.0.0.1:4178 npm run check:seo -- --json` — 17
  rendered routes passed metadata/robots checks (11 public landing/query
  routes, 3 provider profiles and 3 protected route samples).
- Public provider pages generated by ISR/static params:
  `api-proservice-moscow`, `api-autolux-moscow`, `api-formula-moscow`.
- `npm run test:seo` — passed.

Measured local build budgets:

| Budget | Result | Limit |
| --- | ---: | ---: |
| JavaScript total | 3,931.2 kB | 5,500 kB |
| Largest JavaScript asset | 447.5 kB | 600 kB |
| CSS total | 182.1 kB | 500 kB |
| Largest CSS asset | 166.0 kB | 250 kB |
| Public raster media | 6.37 MB | 7 MB |
| Largest public image | 1,710.2 kB | 2 MB |
| Largest map/location image | 245.0 kB | 350 kB |

The checker is available at
[`scripts/check-seo-release.mjs`](../../scripts/check-seo-release.mjs). It
validates title, description, canonical, robots, Open Graph and Twitter tags
when `SEO_BASE_URL` or `--url` is supplied.

## External production gates

- [ ] Run Lighthouse against the deployed production URL and retain the JSON
  report with performance/SEO/accessibility scores.
- [ ] Verify Open Graph previews from the production host for every public and
  configured provider URL.
- [ ] Publish the final provider prerender list from the production build and
  confirm ISR revalidation behavior.

Use `REQUIRE_PRODUCTION_SEO=true` in the release environment to fail closed
until these external checks are attached.
