# Archived Bookly browser checks

These specs exercised the former cabinet-rental product (`/cabinets`, beauty-service fixtures, and cabinet terminology). They are retained for historical migration evidence and are excluded from the active Playwright test directory. AutoCare release checks live in `e2e/autocare-release-audit.spec.ts`.
